# Noral Landing Page - Visual Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         NORAL MONOREPO                           │
│                     (Production-Ready v1.0.0)                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                        📱 APPLICATIONS                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────┐         ┌──────────────────────┐     │
│  │   apps/cms/          │         │   apps/web/          │     │
│  │   (Strapi Backend)   │◄────────┤   (SvelteKit)        │     │
│  │                      │  HTTP   │                      │     │
│  │  • PostgreSQL DB     │  API    │  • SSR Landing Page  │     │
│  │  • Content Types     │         │  • Tailwind CSS      │     │
│  │  • Admin Panel       │         │  • TypeScript        │     │
│  │  • REST API          │         │  • SEO Optimized     │     │
│  │  • TypeScript        │         │  • Responsive UI     │     │
│  └──────────────────────┘         └──────────────────────┘     │
│           :1337                          :3000                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       📦 SHARED PACKAGES                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────┐         ┌──────────────────────┐     │
│  │  packages/config/    │         │  packages/ui/        │     │
│  │  • Tailwind preset   │         │  • Shared components │     │
│  │  • Prettier config   │         │  • Future-ready      │     │
│  └──────────────────────┘         └──────────────────────┘     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      🐳 INFRASTRUCTURE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              Nginx Reverse Proxy (:80)                 │    │
│  │  • Routing       • Caching      • Compression          │    │
│  │  • Security      • Static Files • Health Checks        │    │
│  └──────────────┬──────────────────┬──────────────────────┘    │
│                 │                  │                            │
│        ┌────────▼─────┐   ┌────────▼─────┐                     │
│        │ SvelteKit    │   │   Strapi     │                     │
│        │  Container   │   │  Container   │                     │
│        └──────────────┘   └───────┬──────┘                     │
│                                    │                            │
│                            ┌───────▼──────┐                     │
│                            │  PostgreSQL  │                     │
│                            │  Container   │                     │
│                            └──────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow (Landing Page)

```
┌──────────┐
│  Browser │  User visits http://localhost
└─────┬────┘
      │
      ▼
┌───────────────────────────────────────────────┐
│            Nginx (:80)                        │
│  • Check cache (5min TTL)                     │
│  • Add security headers                       │
│  • Enable gzip compression                    │
└─────┬─────────────────────────────────────────┘
      │
      ▼
┌───────────────────────────────────────────────┐
│         SvelteKit (:3000)                     │
│  +page.server.ts runs on server:              │
│    1. Fetch content from Strapi API           │
│    2. Render HTML with data                   │
│    3. Inject SEO meta tags                    │
└─────┬─────────────────────────────────────────┘
      │
      ▼
┌───────────────────────────────────────────────┐
│         Strapi CMS (:1337)                    │
│  REST API: /api/landing-page                  │
│    • Query PostgreSQL                         │
│    • Populate relations (deep)                │
│    • Return JSON                              │
└─────┬─────────────────────────────────────────┘
      │
      ▼
┌───────────────────────────────────────────────┐
│         PostgreSQL (:5432)                    │
│  • Landing page content                       │
│  • Features collection                        │
│  • Testimonials collection                    │
│  • Media metadata                             │
└─────┬─────────────────────────────────────────┘
      │
      │ (Data flows back up)
      ▼
┌──────────┐
│  Browser │  Receives fully rendered HTML + SEO
└──────────┘
```

---

## 📊 Content Flow

```
┌──────────────────────────────────────────────────┐
│          CONTENT EDITOR WORKFLOW                 │
└──────────────────────────────────────────────────┘

1. LOGIN
   ┌─────────────────┐
   │ Strapi Admin    │  http://localhost/cms/admin
   │ • Create account│
   │ • JWT auth      │
   └─────────┬───────┘
             │
2. CREATE   ▼
   ┌─────────────────────────────────────┐
   │ Content Manager                     │
   │ • Landing Page (Single Type)        │
   │   - Hero section                    │
   │   - Dynamic sections                │
   │   - SEO metadata                    │
   │ • Features (Collection)             │
   │ • Testimonials (Collection)         │
   └─────────┬───────────────────────────┘
             │
3. PUBLISH  ▼
   ┌─────────────────────────────────────┐
   │ Content Status: Published           │
   └─────────┬───────────────────────────┘
             │
4. VIEW     ▼
   ┌─────────────────────────────────────┐
   │ Landing Page                        │
   │ http://localhost                    │
   │ • Content appears immediately       │
   │ • Cache refreshes automatically     │
   └─────────────────────────────────────┘
```

---

## 🎨 Component Architecture

```
┌──────────────────────────────────────────────────┐
│          FRONTEND COMPONENTS                     │
└──────────────────────────────────────────────────┘

/routes/+page.svelte (Landing Page)
     │
     ├─► <Hero />
     │   └─► Data: hero.title, hero.primaryCta, hero.image
     │
     ├─► <FeaturesSection />
     │   └─► Data: section.features[]
     │
     ├─► <TestimonialsSection />
     │   └─► Data: section.testimonials[]
     │
     └─► <CTASection />
         └─► Data: section.title, section.button

Base UI Components:
     ├─► <Container />   (max-width, padding)
     ├─► <Section />     (vertical spacing)
     └─► <Button />      (variants, sizes)
```

---

## 🗂️ Content Types Structure

```
┌──────────────────────────────────────────────────┐
│          STRAPI CONTENT TYPES                    │
└──────────────────────────────────────────────────┘

LANDING PAGE (Single Type)
├── hero (Component)
│   ├── title: string
│   ├── subtitle: string
│   ├── description: text
│   ├── primaryCta: Button component
│   ├── secondaryCta: Button component
│   └── image: media
├── sections (Dynamic Zone)
│   ├── FeaturesSection
│   ├── TestimonialsSection
│   └── CTASection
└── seo (Component)
    ├── metaTitle: string (max 60)
    ├── metaDescription: text (max 160)
    ├── keywords: text
    ├── metaImage: media
    └── canonicalURL: string

FEATURE (Collection Type)
├── title: string
├── description: text
├── icon: string
└── order: integer

TESTIMONIAL (Collection Type)
├── name: string
├── role: string
├── company: string
├── content: text
├── avatar: media
├── rating: integer (1-5)
└── order: integer

COMPONENTS
├── sections/hero
├── sections/features-section
├── sections/testimonials-section
├── sections/cta-section
├── shared/button
└── shared/seo
```

---

## 🚀 Deployment Flow

```
┌──────────────────────────────────────────────────┐
│          DEPLOYMENT PROCESS                      │
└──────────────────────────────────────────────────┘

LOCAL MACHINE
     │
     ├─► 1. Update .env with secrets
     │
     ├─► 2. Run: docker-compose up -d
     │
     ▼
DOCKER COMPOSE
     │
     ├─► Build Stage
     │   ├── Install pnpm
     │   ├── Install dependencies
     │   ├── Build applications
     │   └── Optimize bundles
     │
     ├─► Production Stage
     │   ├── Copy built artifacts
     │   ├── Remove dev dependencies
     │   └── Create small images
     │
     ▼
RUNNING CONTAINERS
     │
     ├─── PostgreSQL (database)
     ├─── Strapi (CMS backend)
     ├─── SvelteKit (frontend)
     └─── Nginx (reverse proxy)
          │
          └──► Port :80 exposed to internet
```

---

## 📈 Performance Optimization Stack

```
┌──────────────────────────────────────────────────┐
│          PERFORMANCE LAYERS                      │
└──────────────────────────────────────────────────┘

BROWSER
     │ Cached assets (1 year)
     ▼
CDN (Future)
     │ Global edge caching
     ▼
NGINX
     │ • Static cache (1 year)
     │ • Page cache (5 min)
     │ • Gzip compression
     ▼
SVELTEKIT
     │ • Server-side rendering
     │ • Code splitting
     │ • Tree shaking
     ▼
STRAPI
     │ • Query optimization
     │ • Connection pooling
     ▼
POSTGRESQL
     │ • Indexed queries
     │ • Connection pool
     └─► Fast data retrieval
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────────────┐
│          SECURITY ARCHITECTURE                   │
└──────────────────────────────────────────────────┘

Layer 1: NGINX
├── Security headers (X-Frame-Options, etc.)
├── Rate limiting (configurable)
├── HTTPS/SSL (production)
└── Request filtering

Layer 2: APPLICATION
├── CORS restrictions
├── Input validation
├── Output sanitization
└── TypeScript type safety

Layer 3: AUTHENTICATION
├── JWT tokens (Strapi admin)
├── Strong password requirements
├── Session management
└── Role-based access control

Layer 4: DATABASE
├── Network isolation (Docker)
├── No direct external access
├── Encrypted connections
└── Regular backups

Layer 5: INFRASTRUCTURE
├── Environment secrets
├── No secrets in code
├── Docker security
└── Minimal attack surface
```

---

**This visual overview provides a complete picture of the Noral Landing Page system architecture, data flows, and organizational structure.**
