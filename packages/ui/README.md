# Shared UI Components

This package is ready for future shared components across multiple apps.

Currently, UI components are defined in each app individually.
As the monorepo grows with Option 2 and beyond, common components can be extracted here.
